/**
 * Previous custom auth (bcrypt + jwt) has been removed in favor of Clerk.
 * Keep this file intentionally empty to avoid accidental usage.
 * All authentication should now be handled via Clerk on the frontend
 * and verified server-side using `backend/middleware/auth.js`.
 */

module.exports = {};
